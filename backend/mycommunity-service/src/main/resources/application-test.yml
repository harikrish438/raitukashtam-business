spring:
  config:
    import:
      - "vault://"
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    org.springframework.web: INFO
    com.raitukashtam: DEBUG
    io.micrometer.tracing: INFO

package com.raitukashtam.mycommunity.response;

/** Discriminator for DashboardResponse's derived/union Recent Activity feed -- not a persisted entity. */
public enum ActivityType {
    PAYMENT,
    ANNOUNCEMENT
}

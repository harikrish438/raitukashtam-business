# TEST environment — shared infrastructure for raitukashtam-business
# run with (from the repo root):
#   docker compose -f docker-compose.test.yml --env-file .env.test up -d
# Requires: .env.test file (copy from .env.test.example).
name: raitukashtam-business-test

networks:
  raitukashtam-network:
    driver: bridge
    name: raitukashtam-network

volumes:
  business-redis-data-test:

services:

  vault:
    image: hashicorp/vault:1.17
    container_name: vault-test
    restart: unless-stopped
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: ${VAULT_TOKEN:?VAULT_TOKEN is required in .env.test}
      VAULT_DEV_LISTEN_ADDRESS: 0.0.0.0:8200
      TZ: Asia/Kolkata
    cap_add:
      - IPC_LOCK
    networks:
      - raitukashtam-network
    healthcheck:
      test: ["CMD", "vault", "status", "-address=http://localhost:8200"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  vault-init:
    image: hashicorp/vault:1.17
    container_name: vault-init-test
    restart: "no"
    depends_on:
      vault:
        condition: service_healthy
    environment:
      VAULT_ADDR: http://vault-test:8200
      VAULT_TOKEN: ${VAULT_TOKEN:?VAULT_TOKEN is required in .env.test}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required in .env.test}
      JWT_SIGNING_KEY_PRIVATE: ${JWT_SIGNING_KEY_PRIVATE:?JWT_SIGNING_KEY_PRIVATE is required in .env.test}
      JWT_SIGNING_KEY_PUBLIC: ${JWT_SIGNING_KEY_PUBLIC:?JWT_SIGNING_KEY_PUBLIC is required in .env.test}
      AUTH_DB_PASSWORD: ${AUTH_DB_PASSWORD:?AUTH_DB_PASSWORD is required in .env.test}
      MYCOMMUNITY_DB_PASSWORD: ${MYCOMMUNITY_DB_PASSWORD:?MYCOMMUNITY_DB_PASSWORD is required in .env.test}
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required in .env.test}
      MAIL_PASSWORD: ${MAIL_PASSWORD:?MAIL_PASSWORD is required in .env.test}
      RECAPTCHA_SECRET_KEY: ${RECAPTCHA_SECRET_KEY:?RECAPTCHA_SECRET_KEY is required in .env.test}
      TWOFACTOR_API_KEY: ${TWOFACTOR_API_KEY:?TWOFACTOR_API_KEY is required in .env.test}
    volumes:
      - ./infra/vault/vault-init.sh:/vault-init.sh:ro
    entrypoint: ["/bin/sh", "/vault-init.sh"]
    networks:
      - raitukashtam-network

  redis:
    image: redis:7-alpine
    container_name: redis-test
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD is required in .env.test}
    volumes:
      - business-redis-data-test:/data
    networks:
      - raitukashtam-network
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a ${REDIS_PASSWORD} ping | grep PONG"]
      interval: 5s
      timeout: 30s
      retries: 5
